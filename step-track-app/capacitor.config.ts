import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'step-track-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
